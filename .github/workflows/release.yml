name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Build skill files
        run: make build

      - name: Bundle skill files
        run: cd dist && zip claude-skills.zip *.skill

      - name: Create GitHub Release
        run: |
          gh release create ${{ github.ref_name }} dist/claude-skills.zip \
            --title "Release ${{ github.ref_name }}" \
            --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
